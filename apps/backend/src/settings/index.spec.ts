describe( 'Settings', () => {
  describe( 'on client connection', () => {
    it.todo( 'should send existing settings to the client' )
  } )

  describe( 'on updated settings', () => {
    it.todo( 'should send the updated settings to all clients' )

    describe( 'client settings', () => {
      it.todo( 'should ignore changes set on private clients' )
      it.todo( 'should omit private clients from settings' )
    } )

    describe( 'global settings', () => {
      it.todo( 'should save the global settings to file' )
    } )
  } )
} )
