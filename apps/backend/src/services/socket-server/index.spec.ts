describe( 'Socket Server', () => {
  describe( 'on connection', () => {} )

  describe( 'on connection ready', () => {
    it.todo( 'should only send' )
  } )

  describe( 'broadcast', () => {} )

  describe( 'heartbeat', () => {
    it.todo( 'should force close connections which do not respond' )
  } )
} )
